ques 6 write a python program to rotate a list one position to the right
lst = list(map(int, input("Enter list elements: ").split()))

if len(lst) > 0:
    lst = [lst[-1]] + lst[:-1]

print("List after right rotation:", lst)'''
